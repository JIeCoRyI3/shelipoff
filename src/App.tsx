import Home from 'pages/Home';
import React, { FC } from 'react';

import * as Styled from './styles';

const App: FC = () => {
	return (
		<Styled.App>
			<Home />
		</Styled.App>
	);
};

export default App;
